# API routes

Create one Express router per resource: auth, companies, jobs, applicants, resumes, applications, interviews, offers, recommendations, reports, audit logs, and notifications.

Routes translate HTTP requests into application use-case calls. Business rules do not belong in this layer.
