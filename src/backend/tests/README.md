# Tests

- `unit/`: domain rules and application use cases with mocked ports.
- `integration/`: HTTP routes, PostgreSQL repositories, and complete business flows.

Vitest is the test runner for both suites.
